#include <iostream>
#include <stdio.h>
using namespace std;

int main()
{
    int i, j, param, k, count;
    cout << "Enter parametr" << endl;
    cin >> param;
    k=param%2;
    count=param/2;
    if (k!=0)
    {
        for (j=1; j<param+1; j++)
        {
            
            if (j%2!=0)
            {
                for (i=0; i<(count); i++)
                {
                   cout << " ";
                }
                for (int l=0; l<j; l++)
                {
                   cout << "*"; 
                }
                 count = count - 1;
            }
           
            if (j%2==0)
            {
                cout << endl<< endl;
            }
        }
    }
    if (k==0)
    {
        cout << endl;
        for (j=2; j<param+1; j++)
        {
            
            if (j%2==0)
            {
                for (i=0; i<(count); i++)
                {
                   cout << " ";
                }
                for (int l=0; l<j; l++)
                {
                   cout << "*"; 
                }
                 count = count - 1;
            }
           
            if (j%2!=0)
            {
                cout << endl<< endl;
            }
        }
    }
    return 0;   
}


#include <iostream>
#include <stdio.h>
#include <math.h> 
using namespace std;

int main()
{
    float MaxHigh, LocalTime = 0,  High, Interval, Velocity;
   
    int K;
    cin >> MaxHigh; 
    cin >> Interval;
    High = MaxHigh;
    
    while (High > 0)
    {
        LocalTime = LocalTime + Interval;
        High = MaxHigh - (9.81*LocalTime*LocalTime)/2;
        Velocity = 9.81*LocalTime;
        if ( High > 0 ) //&& ( fmod (LocalTime))
        {
            cout << High << "  "<<  Velocity << endl;
        }
        
    }
    
    
    
    return 0;
}

